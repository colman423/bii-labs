{
  "name": "bii-labs",
  "version": "0.0.1",
  "repository": "https://github.com/colman423/bii-labs.git",
  "license": "MIT",
  "scripts": {
    "start": "gulp",
    "build": "gulp build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-clean": "^0.4.0",
    "gulp-rename": "^2.0.0",
    "gulp-sass": "^4.0.2",
    "gulp-uglify": "^3.0.2",
    "node-sass": "^4.13.1"
  },
  "dependencies": {
    "gulp-webserver": "^0.9.1"
  }
}
